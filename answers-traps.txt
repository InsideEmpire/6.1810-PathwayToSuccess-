1. The registers a0 ~ a7. Register a2 holds the parameter 13.
2. There is no actual call to f or g in the assembly. The compiler inlined both functions and directly computed 8 + 3 + 1.
3. <printf> is at address 0x65a.
4. The return address register ra contains the address of the next instruction, which is 0x40.
5. The output is He110 World.
6. y=%d prints a garbage value (random stack contents).